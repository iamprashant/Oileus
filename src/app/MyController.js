/**
 * Example for Oileus controller.
 */

/**
 * Do init here
 * @constructor
 */
oileus.controller("MyController", function() {
	//code
})
/**
 * Handles when the register loaded. The expected structure of response is:
 * { status, html }
 */
.actFor("MyController",
"activator", function(info, el) {
	//code
});